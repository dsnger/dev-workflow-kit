BLOCKER | high | Opening protocol paragraphs and spec §6 Validation | the plan says the scratch checks never enter Gate B and claims that residual is recorded in spec §8, but spec §6 says their executable form is reviewed by Gate B and §8 records no unreviewed-harness residual | this contradicts both the settled spec and the plan's own rule that §6 governs outside the four enumerated held items, leaving the only executable checks outside the promised review | make the executable checks reviewable in the Gate-B range, or reopen the spec to record and approve this residual before execution
BLOCKER | high | Task 4 step 7 C1b counter-check | the step changes one character of the real 2026-07-20 ledger row and later restores it | §2.1 makes a row immutable as soon as it exists, committed or not, so the validation procedure itself violates the convention it is implementing | run the mutation against a scratch copy or injected stand-in derived from the real ledger and leave docs/hardening-log.md untouched
BLOCKER | high | Task 5 step 4 counter-checks | moving the real block and appending then reverting a second complete entry mutate or remove protected entries after Task 4 has created and committed them | §2.2 says complete entries are never edited or removed even before commit, so these counter-checks execute forbidden states in the product ledger | apply every entry-affecting C3 mutation to scratch ledger fixtures and keep the real ledger unchanged
BLOCKER | high | Global staging constraint and Task 8 step 3 | the consolidation sequence uses soft reset's implicitly broad staged result and commits it without an explicit tracked-path staging operation, while Task 8 declares no modified files and therefore supplies no step file list against which the mandated exact-set assertion can run | this reopens the pass-2 staging blocker and can fold an unintended post-BASE path into the sole Gate-B snapshot | name the five intended tracked paths for the consolidation, stage only those explicit pathspecs, and assert the cached path set equals that list before committing
MAJOR | high | Fixture matrix ledger-unreadable row, Task 4 step 3, and Task 5 step 3 | ledger-unreadable promises failure for every label that reads the ledger, but its Expected cell names only C3; Task 4 therefore excludes it from the C1a/C1d run and Task 5 verifies only C3 | the read-failure oracle for C1a, C1b, C1c, C1d, C2a, and C2b is never exercised despite the matrix claiming it is | list every affected label explicitly and route the fixture through each label after that label exists
MAJOR | high | Task 4 step 3 and Task 5 step 3 | the matrix declares an expected outcome under both sh and dash, but neither step requires its fixture rows to run under both shells | a shell-specific parser defect can survive the fixture evidence even though the real-ledger happy path passes twice | require every applicable matrix row to be evaluated under both sh and dash
MINOR | high | Mechanical sweep — Task 2 step 1 | the rationale says a multiline pattern passed to grep -F cannot match and reports a clean absence, but fixed-string grep treats embedded newlines as separate patterns and can exit 0 after matching either line | the false mechanism claim can lead the executor to self-test the wrong failure mode | state the actual oracle: a multiline fixed pattern does not establish one contiguous multiline sequence and may produce false positives by matching its component patterns
MINOR | high | Mechanical sweep — The thirty-five anchors | the plan says two anchors begin with a hyphen, but only `- <date> · supersedes <row date>` does | this is a mechanically false count in the data-handling rationale and the Task 1 sweep does not catch it | change the claim to one anchor or add the missing second adversarial anchor if two are required
MINOR | high | The checks — labels, properties, oracles | the C1e rationale says the check can only be made to fail by mutating the table by hand, although an unreadable or missing ledger is another failure path for a correct chronology check | the totality claim overstates the reason C1e lacks change-specific evidence | use the spec's narrower wording that C1e's falsifying observation requires a table mutation and that this diff cannot produce it
END OF FINDINGS (9 total)
