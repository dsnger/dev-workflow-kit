BLOCKER | high | plan Task 5 Interfaces/Step 3 and Gate B, lines 472-504 and 540-546 | `WIP_PARENT` is declared locally derived, but Task 5 says it consumes the value from Task 1, splits assignment and use across separate command fences, and the fix loop invokes the bare variable without re-deriving it | A resumed session or separate tool call has no such shell variable, so the decisive plugin range check can fail operationally, use an empty value, or cease to prove the reviewed range | Put the WIP-subject guard, `WIP_PARENT=$(git rev-parse HEAD^)`, non-empty plugin-range guard, and version checker in one self-contained command at every use, including every Gate-B iteration
MAJOR | high | spec §5.2 lines 469-479 and scripts/check-invariants.sh lines 480-487 | Whole-file exactly-once counting is described as the same guarantee and strictly stronger than the bounded check, but a lone canonical line moved outside `CLAUDE.md` §5 or outside the scaffolded CLAUDE template still passes | In the command file, text outside the inline template is not shipped to initialized projects, so 4c can be green while the product copy lacks the writer rule; the lost placement guarantee is not priced | State that the amendment trades away placement enforcement as well as tightening duplicate detection, decide explicitly that file-level presence is sufficient, and add an accept fixture for a lone out-of-template occurrence; otherwise retain a trustworthy template-placement check
MAJOR | high | spec §4 line 392 and §5.2 fixture table lines 489-501 | The amended spec still requires bounded §5 fixture regions, ignores an outside duplicate, and tests start/end boundary cardinality even though the settled checker has no regions or boundaries | The approved spec now gives mutually exclusive expected results and the plan's claim of complete §4/§5.2 coverage is false | Rewrite the site row and fixture table around the actual whole-file cases, removing every in-region, outside-ignored, start-boundary, end-boundary, and multi-boundary requirement
MAJOR | high | plan Task 4 lines 394-450 versus todos.md lines 367-384 | The plan says the gate-cycle slot-collision row does not exist and orders a seventh new row, but `todos.md` already contains that exact open item and trigger | Execution would duplicate one backlog concern instead of preserving and updating its existing history, and all new-row counts and closing-message claims would be wrong | Amend the existing slot-reuse row with this story's newly fired occurrence, reduce the new-row count from seven to six, and update Task 4's closing-message share
MAJOR | high | plan Task 2 Step 4 and Self-review, lines 338-341 and 595-637 | The plan still claims 4c checks region boundaries, directs a heading-boundary falsification, names obsolete `file, mode` and sentinel returns, cites `CANON`, counts seven failure messages, and cites the removed `sev-region-parser` seam | These are assertions about a checker that does not exist and make the final self-review and evidence record false despite the implemented checker being green | Replace the entire stale cluster with the real fixed-path whole-file contract: `SEV_CANON`/`SEV_LINE`, one file argument, numeric count plus awk status, four diagnostic branches, and the `sev-canon-count` injection seam
MAJOR | medium | plan preconditions and Task 1 Step 8 | The amended spec is currently modified and the plan is untracked, but the plan never requires the approved documentation state to be committed before creating the WIP and Task 1's explicit staging list excludes both | If executed literally, the WIP parent can predate the settled amendment, Gate B excludes it from the reviewed range, and the cycle can close with the authoritative spec and plan still dirty | Add a pre-Task-1 prerequisite that the approved spec amendment and plan are committed as the Gate-A documentation state, then derive the WIP parent from the subsequent WIP snapshot
MINOR | high | spec §5.2 lines 464-467 | The contract says every failure, including missing or unreadable input, exits naming the file and the count, while the real missing and unreadable branches name a cause and provide no count | This overstates the exact diagnostic comparison and violates the repository's gate-proof calibration rule | Say cardinality failures name the file and count, while missing, unreadable, and parser failures name the file and cause
MINOR | high | plan Task 1 file inventory line 84 and Step 7 | The mutation-evidence block is listed as still to modify even though the tree already contains the re-measured 20/22/13 block and Step 7 says it is already measured and recorded | The plan does not accurately distinguish completed working-tree work from remaining edits, inviting an unnecessary rewrite of evidence that already matches the observed run | Mark the mutation block as already modified and make Step 7 verification-only unless a later change fires its re-run trigger
MINOR | high | plan Global Constraints lines 61-64 and Task 5 lines 493-497 | The plan says the closing amend makes `HEAD^` name something else entirely, but amending the WIP replaces the commit while preserving its parent | The WIP guard is useful, but its stated git-mechanics reason is false and can mislead later sequencing changes | Say the subject guard prevents using the procedure after the active WIP state has ended; do not claim the parent changes merely because the commit was amended
END OF FINDINGS (9 total)
