MAJOR | high | Mechanical sweep — C2a oracle | the prose says two anchors begin with `-`, while the counted list and the adjacent count claim show exactly one | the executor receives contradictory option-safety requirements in the oracle for the anchor checker | change `two begin` to `one begins`
BLOCKER | high | Spec §8 "Four things found" introduction, consumed by Task 1 | it still says Gate B reviews the plan and executable checks against the real diff, although §6 and the new §8 residual say neither scratch checks nor this plan are inside that range | Task 1 would commit a spec that contradicts the exact review boundary the plan relies on | replace the sentence with the settled division: Gate B compares the implementation range, while scratch check source is only supplied as additional context and is not range-reviewed
BLOCKER | high | Plan opening review-boundary paragraph and Task 8 step 4 | the opening and spec §8 say Task 8 supplies the scratch check source to the reviewer, but Task 8's `additionalContext` list contains only the story path, evidence entry, and falsification lens | the checks receive neither the promised human read nor the only review guard the recorded residual claims for them | require `additionalContext` to include the complete current harness and check source, plus enough fixture definitions to judge their claimed distinctions
BLOCKER | high | Task 8 steps 4–6 and Self-Review placeholder scan | step 4 requires the current evidence entry quoted verbatim, but no preceding step composes that entry, and the placeholder scan defers it until the closing amend after the review call | CLAUDE.md §5 requires the evidence entry before every Gate-B call, so the sequence cannot execute as written without inventing an unstated artifact at the call site | add a pre-call step that composes and validates the exact evidence entry from the battery, named failing check, and counterfactual, then revalidate that same entry at each cited §5 checkpoint
MAJOR | high | Task 1 steps 1 and 3; Task 8 steps 3, 5, and 6 | the plan copies §5-owned prose-exemption, WIP/amend, stale-range, and evidence-revalidation protocol while simultaneously claiming §5 is cited rather than restated | duplicated protocol can drift and violates the explicit cite-only rider for this plan | strip the copied protocol explanations and mechanics, retain only change-specific values and staging constraints, and cite the exact CLAUDE.md §5 subsections for the procedure
MAJOR | high | C1d.2 oracle and Self-Review §8 item 4 trace | both claim the candidate interval closes the format-example residual, but C1d deliberately ignores entry-shaped lines outside that interval; only C3's whole-file rule and Task 5 below-table counter-check detect a flush-left dated example | the residual is attributed to evidence that would pass the forbidden state, overstating what C1d proves | state that C1d.2 excludes the example from candidates and that C3 plus Task 5's outside-interval counter-check supplies the executable guard; trace both in Self-Review
MAJOR | high | Task 8 step 3 | after `git reset --soft $BASE`, explicitly adding the five intended paths does not remove any extra path already staged by the soft reset | in the unexpected accumulated-path state the step itself warns about, the equality assertion stops execution but the plan gives no safe route to create the required WIP commit | clear the index while preserving the worktree before explicitly staging the five paths, or explicitly unstage every validated extra path before the set assertion
MINOR | high | Global Constraints versus Task 2 step 3 | the global rule says to assert exit status and never printed output, while the harness self-test is required to assert that a deliberate failure was printed | the plan gives the executor incompatible evidence rules for the self-test | scope the no-output rule to property and fixture outcomes, and explicitly carve out the harness meta-test's diagnostic-output assertion
END OF FINDINGS (8 total)
