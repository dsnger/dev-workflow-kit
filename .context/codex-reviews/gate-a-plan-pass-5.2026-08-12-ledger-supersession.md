MINOR | high | Mechanical sweep — Task 4 Step 5 lines 405–416 | the statement that everything except `$D` is verbatim from spec §3.1 also covers the preceding `**Superseded rows:**` label, but §3.1 contains only the entry line and not that label | the quoted-source claim is mechanically false and obscures that the label comes from §2.2 | limit the verbatim claim to the entry line and cite §2.2 separately for the label
BLOCKER | high | Read pass — Task 1 Step 4 and Task 8 Steps 3 and 5 | `BASE` is the commit containing the plan, then Gate B reviews `$BASE..HEAD`, which excludes that commit even though spec §8 now says the committed plan falls inside the implementation range | the pass-4 correction is absent from its plan mirror and Gate B cannot range-review the plan as promised | keep the Task-1 commit as a separate check baseline, record its parent as a review baseline, consolidate implementation onto the Task-1 commit, and call Gate B from the review baseline so the artifact commit and implementation commit are both in range
MAJOR | high | Read pass — Task 2 Step 5 | it says the self-test, shellcheck, and counter-checks are the harness's only review, contradicting the opening contract, Task 8 Step 5, and spec §8, which all require the Gate-B reviewer to read the complete harness and check source in `additionalContext` | the plan gives two incompatible accounts of the only review available to uncommitted executable evidence | add the Task-8 reviewer read to this list and reserve “outside the reviewed range” for the narrower claim that remains true
MAJOR | high | Read pass — fixture matrix `entry-outside-block` and Task 4 Step 3 | “entry-shaped line below the table” does not say whether the mandated entry was moved there or an extra line was added while the mandated entry remains in the valid block; the first construction fails `C1d`, while the second must pass under `C1d`.1 and `C1d`.2 | Task 4 cannot build the fixture with a determinate expected `C1d` result, and choosing the second construction would make the new scope remedy appear broken | define this fixture as the mandated entry moved below the table with no mandated entry inside the block, leaving Task 5's separate extra-below-table counter-check to exercise the second construction
MAJOR | high | Read pass — Task 4 Step 7 | `C1c` mutates each real surface and restores it but never reruns `C1c` after the final restore before Task 4 commits | a failed or incomplete restore can be committed even though the last green `C1c` result predates the mutation, violating the untouched-header requirement in both mirrors | run `C1c` on the restored real surfaces under both shells immediately before Step 8, or perform all three mutations on scratch copies and finish with a green real-surface run
MINOR | high | Read pass — Task 8 Step 3 lines 579–581 | the plan restates the §5 WIP-prefix hook behavior after citing Mechanics, despite the standing rider requiring §5 protocol content to be cited rather than copied | this creates another protocol mirror whose wording can drift from `CLAUDE.md` §5 | replace the explanatory restatement with a direct citation to `CLAUDE.md` §5 Mechanics while retaining only the plan-specific `WIP:` value
END OF FINDINGS (6 total)
